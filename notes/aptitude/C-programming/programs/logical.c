#include <stdio.h>
    int main() 
    {
        int a = 11, b=5, c=10;
        if(a>b && a>c) {
           printf("\n a is greater than both");
        } else {
           printf("\n a is not greater than both");
        }
        return (0);
    }

    // a is greater than both